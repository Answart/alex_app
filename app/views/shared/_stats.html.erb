<!-- SOURCE: app/views/shared/_stats.html.erb
A partial for displaying follower stats.
This does nothing when @user is not nil (as on a profile page), but when it is (as on the Home page) it sets @user to the current user
-->

<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <!-- for the benefit of the Ajax implementation in Section 11.2.5, which accesses elements on the page using their unique ids -->
    <strong id="following" class="stat">
      <%= @user.followed_users.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <!--  -->
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
</div>